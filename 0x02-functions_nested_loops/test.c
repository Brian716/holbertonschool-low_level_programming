#include <stdio.h>

/**
 * main - function prints exact phrases
 * Return: 0
 */

int main(void)
{
	char a = 'a';
	int b = (int)a;
	int c = 'H';
	printf("%d, \n", c);
	printf("%d,\n", b);
	int r = 1;
	putchar(r);
	putchar('\n');
	return (0);
}
